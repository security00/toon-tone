import SeoGamePage from "@/components/SeoGamePage";

export default function Page() {
  return <SeoGamePage title="Play Toon-Tone Online" intro="Start the free browser version of Toon-Tone. No app download or login required — just match cartoon character colors with HSB sliders." />;
}
